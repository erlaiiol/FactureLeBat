{
  "name": "facturelebat",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^10.7.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.1.0",
    "prettier": "^3.9.6"
  },
  "lint-staged": {
    "backend/**/*.ts": [
      "prettier --write",
      "eslint --fix"
    ],
    "frontend/**/*.{ts,html,scss}": [
      "prettier --write"
    ]
  }
}
